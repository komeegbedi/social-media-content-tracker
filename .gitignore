# dependencies & build
node_modules
dist
.vite

# secrets & local config
.env
.env.local
.env.*.local

# Firebase emulator / CLI artifacts
.firebase
*-debug.log
*.log
firebase-export-*
.runtimeconfig.json

# real team data — keep out of the (public) repo
production-tasks.csv
images/

# OS / editor
.DS_Store
